return{
    {
        "ray-x/lsp_signature.nvim",
        event = "InsertEnter",
        opts = {
            -- cfg options
            floating_window = false, -- disable auto popup
            hint_enable = false,
        },
    }
}
